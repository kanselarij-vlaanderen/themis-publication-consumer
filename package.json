{
  "name": "themis-publication-consumer",
  "version": "2.2.0",
  "description": "Microservice that ingests delta files for Themis publications",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kanselarij/themis-publication-consumer.git"
  },
  "author": "redpencil.io",
  "license": "MIT",
  "dependencies": {
    "@lblod/mu-auth-sudo": "^0.3.2",
    "node-fetch": "^2.6.0",
    "fs-extra": "^8.0.1"
  }
}
